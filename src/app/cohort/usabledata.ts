export class UsableData {
    description: string;
    category: string;
    role: string;
    cohort: string;

    constructor(description: string, category: string, role: string, cohort: string) {
        this.description = description,
        this.category = category,
        this.role = role,
        this.cohort = cohort
    }
}
